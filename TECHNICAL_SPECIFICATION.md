# Техническая спецификация — mh-compressor-manager v2.0

**Версия документа:** 3.0  
**Стандарт C++:** C++23  
**Платформа:** Linux (Fedora 38+)  
**Статус:** Утверждено  
**Год:** 2026  

---

## 📑 Содержание

1. [Общие сведения](#1-общие-сведения)
2. [Назначение и принципы работы](#2-назначение-и-принципы-работы)
3. [Требования к функциональности](#3-требования-к-функциональности)
4. [Требования к минимальному размеру файла для сжатия](#4-требования-к-минимальному-размеру-файла-для-сжатия)
5. [Требования к конфигурации](#5-требования-к-конфигурации)
6. [Интерфейс командной строки (CLI)](#6-интерфейс-командной-строки-cli)
7. [Требования к надежности и производительности](#7-требования-к-надежности-и-производительности)
8. [Требования к безопасности](#8-требования-к-безопасности)
9. [Интеграция с системой (Systemd)](#9-интеграция-с-системой-systemd)
10. [Логирование и отчетность](#10-логирование-и-отчетность)
11. [Требования к разработке и поставке](#11-требования-к-разработке-и-поставке)
12. [Критерии приемки](#12-критерии-приемки)
13. [История разработки](#13-история-разработки)
14. [Правообладатель](#14-правообладатель)

---

## 1. Общие сведения

| Параметр | Значение |
|----------|----------|
| **1.1. Наименование программы** | `mh-compressor-manager` |
| **1.2. Язык программирования** | C++ (стандарт не ниже **C++23**) |
| **1.3. Компилятор** | GCC 13+ или Clang 16+ |
| **1.4. Целевая платформа** | Операционная система Linux (приоритет: **Fedora 38+**, также поддержка CentOS, Ubuntu) |
| **1.5. Тип приложения** | Консольная утилита / Фоновая служба (Daemon) |
| **1.6. Миссия программы** | Автоматическое сжатие статического контента для веб-сервера nginx с целью снижения сетевого трафика и ускорения загрузки страниц |

---

## 2. Назначение и принципы работы

> **✅ Основная цель:** Автоматизация процесса сжатия статических файлов для оптимизации работы веб-сервера.

### 2.1. Мониторинг

Программа отслеживает изменения в файловой системе в режиме реального времени (мониторинг).

### 2.2. Обработка файлов

При обнаружении новых или измененных файлов с подходящими расширениями программа создает их сжатые копии в той же директории.

### 2.3. Алгоритмы сжатия

| Алгоритм | Расширение | Библиотека |
|----------|-----------|------------|
| **Gzip** | `.gz` | `zlib` |
| **Brotli** | `.br` | `libbrotli` |

### 2.4. Сохранность данных

Исходные файлы **не удаляются** и **не модифицируются**.

### 2.5. Метаданные

Сжатые копии должны наследовать от оригинала:
- Время создания
- Права доступа (permissions)
- Контекст безопасности SELinux
- Владельца и группу (UID/GID)

### 2.6. Архитектура

| Компонент | Назначение |
|-----------|-----------|
| **Основной процесс** | Демон, работащий в фоне |
| **Поток-диспетчер** | Обрабатывает события файловой системы (inotify) |
| **Пул потоков** | Выполняет операции сжатия асинхронно |
| **Systemd-интеграция** | Взаимодействие через уведомления о готовности (`sd_notify`) |

---

## 3. Требования к функциональности

### 3.1. Мониторинг файловой системы

#### 3.1.1. Рекурсивное сканирование

Программа должна рекурсивно сканировать указанные целевые директорторию(ии).

#### 3.1.2. Механизм отслеживания

Используется механизм `inotify` (API или библиотека) для отслеживания событий:
- Создание файлов
- Модификация файлов
- Удаление файлов

#### 3.1.3. Обработка событий

| Событие | Действие |
|---------|----------|
| **Создание/Изменение** | Если файл имеет подходящее расширение, планируется задача на сжатие |
| **Удаление** | Если удаляется исходный файл, соответствующие сжатые копии (`.gz`, `.br`) также должны быть удалены |
| **Игнорирование** | Программа не должна следовать за символическими ссылками (symlinks) |
| **Фильтрация** | Не обрабатывать файлы, которые уже являются сжатыми копиями (оканчиваются на `.gz` или `.br`) |

#### 3.1.4. Debounce (Задержка)

> **📌 Требование:** Для предотвращения множественной обработки одного файла при частых изменениях (например, при копировании), сжатие выполняется только после того, как файл не изменялся в течение заданного интервала (параметр `debounce_delay`).

#### 3.1.5. Идемпотентность

Если сжатый файл уже существует и его время модификации новее или равно времени модификации исходного файла, повторное сжатие не производится (при первоначальном сканировании).

### 3.2. Алгоритмы сжатия

#### 3.2.1. Уровень сжатия

Уровень сжатия для Gzip и Brotli должен быть настраиваемым.

#### 3.2.2. Многопоточность

Поддержка многопоточности: параллельное сжатие разных файлов через пул потоков.

#### 3.2.3. Ограничение нагрузки

Количество параллельных операций сжатия не должно превышать заданное значение (параметр `threads`).

### 3.3. Расширения файлов

#### 3.3.1. Список расширений по умолчанию

```
txt, js, css, svg, json, html, htm, map
```

#### 3.3.2. Регистронезависимость

Сравнение расширений должно быть регистронезависимым (`.JS` = `.js`).

#### 3.3.3. Пользовательская конфигурация

Пользователь может изменять список расширений в конфигурации.

---

## 4. Требования к минимальному размеру файла для сжатия

> **📋 Основание:** Исследование минимального размера файла для сжатия gzip/brotli (см. `Исследование_минимальный_размер_файла_gzip_brotli.md`)

### 4.1. Пороговые значения

| Параметр | Значение | Обоснование |
|----------|----------|-------------|
| `MIN_COMPRESS_SIZE` | **256 байт** | Файлы менее 150 байт могут увеличиться после сжатия; 256 байт — практический оптимум |
| `OPTIMAL_COMPRESS_SIZE` | **1024 байт (1 КБ)** | Файлы ≥1 КБ дают значительный выигрыш от сжатия |

### 4.2. Стратегия сжатия по размерам файлов

| Размер файла | Действие | Приоритет |
|--------------|----------|-----------|
| **< 256 байт** | **Не сжимать** | Высокий |
| **256–1023 байт** | **Сжимать** | Средний |
| **≥ 1024 байт** | **Сжимать обязательно** | Высокий |

### 4.3. Требования к логированию малых файлов

При пропуске файлов размером менее 256 байт система должна:
- Логировать событие на уровне `DEBUG` (не `WARNING` и не `ERROR`)
- Указывать причину: `file size below minimum threshold (256 bytes)`
- Не создавать предупреждения для пользователя

**Пример:**
```
[DEBUG] Пропуск файла /srv/www/small.js: размер 128 байт < минимального порога (256 байт)
```

### 4.4. Требования к конфигурации

Параметры должны быть настраиваемыми через конфигурационный файл:

```ini
[compression]
min_compress_size = 256
optimal_compress_size = 1024
```

**Требования:**
- Значения по умолчанию: 256 и 1024 байт соответственно
- Минимально допустимое значение `min_compress_size`: 128 байт
- Минимально допустимое значение `optimal_compress_size`: 512 байт
- При некорректных значениях использовать умолчания с логированием предупреждения

### 4.5. Проверка размера файла

**Псевдокод:**
```cpp
bool should_compress(size_t file_size) {
    if (file_size < m_cfg.min_compress_size) {
        return false;
    }
    return true;
}
```

**Требования:**
- Проверка выполняется **до** начала сжатия
- Проверка выполняется **после** открытия файла и получения его размера
- Не пытаться сжимать файлы ниже порога

### 4.6. Статистика пропущенных файлов

Система должна вести счётчики:
- `files_skipped_small` — количество пропущенных файлов (размер < порога)
- `bytes_skipped_small` — общий объём пропущенных данных (байт)

Эти счётчики должны быть доступны в:
- Выводе статуса сервиса
- Метриках (если реализовано)
- Логах статистики при завершении работы

### 4.7. Предварительное сжатие в фоне

**Контекст:** mh-compressor-manager выполняет сжатие **предварительно** (не на лету) с использованием **максимальной степени сжатия**:
- **gzip:** уровень 9
- **brotli:** уровень 11

**Вывод:** Затраты процессора на сжатие не являются критичными, так как выполняются в фоне. Однако распаковка на клиенте остаётся фактором, поэтому порог 256 байт сохраняет актуальность.

### 4.8. Совместимость с nginx

Настройки mh-compressor-manager должны быть совместимы с рекомендациями nginx:
- `gzip_min_length 256;`
- `brotli_min_length 256;`

Это обеспечивает единообразие обработки файлов на обоих уровнях.

---

## 5. Требования к конфигурации

| Параметр | Значение |
|----------|----------|
| **5.1. Путь к конфигурационному файлу** | `/etc/mediahive/compressor-manager.conf` |
| **5.2. Формат** | INI |
| **5.3. Приоритет настроек** | Аргументы командной строки имеют приоритет над конфигурационным файлом |

### 5.4. Структура файла конфигурации

```ini
[general]
# Каталоги для мониторинга (разделитель ; или пробел)
target_path=/var/www/html;/var/www/site1

# Логирование отладки (true/false)
debug=false

# Количество потоков (0 = авто)
threads=0

# Список расширений (без точек, через пробел)
list=txt js css svg json html htm map

# Алгоритмы (gzip, brotli, all)
algorithms=all

# Уровень сжатия Gzip (1-9)
gzip_level=6

# Уровень сжатия Brotli (1-11)
brotli_level=4

# Задержка перед сжатием после изменения файла (сек)
debounce_delay=2

# Минимальный размер файла для сжатия (байт)
min_compress_size=256

# Оптимальный размер файла для сжатия (байт)
optimal_compress_size=1024

# Опционально: переопределение настроек для конкретной папки
[/var/www/site2]
list=html htm
algorithms=all
gzip_level=6
brotli_level=4
```

### 5.5. Отсутствие конфигурационного файла

> **⚠️ Важно:** При отсутствии конфигурационного файла программа использует значения по умолчанию и записывает предупреждение в лог.

---

## 6. Интерфейс командной строки (CLI)

Программа должна поддерживать следующие аргументы:

| Аргумент | Описание | Приоритет |
|----------|----------|-----------|
| `--config <path>` | Путь к файлу конфигурации (по умолчанию `/etc/mediahive/compressor-manager.conf`) | Высокий |
| `--dir <path>` | Целевая директория (переопределяет `target_path`) | Высокий |
| `--ext <list>` | Список расширений (переопределяет `list`) | Высокий |
| `--gzip-level <N>` | Уровень сжатия Gzip | Высокий |
| `--brotli-level <N>` | Уровень сжатия Brotli | Высокий |
| `--min-size <N>` | Минимальный размер файла для сжатия (переопределяет `min_compress_size`) | Высокий |
| `--dry-run` | Режим проверки (показать планы без действий) | — |
| `--help`, `-h` | Вывод справки | — |
| `--version`, `-v` | Вывод версии | — |

---

## 7. Требования к надежности и производительности

### 7.1. Ресурсные ограничения

| Ресурс | Ограничение |
|--------|-----------|
| **Потребление ОЗУ (покой)** | Не более **50 МБ** |
| **Потребление ОЗУ (пик)** | До **200 МБ** |
| **Файловые дескрипторы** | Должно учитываться ограничение `inotify` (рекурсивное добавление наблюдений) |

### 7.2. Обработка ошибок

- Корректная обработка отсутствия прав на чтение/запись
- Обработка переполнения диска
- Обработка разрыва соединения `inotify` (превышение лимита событий) с попыткой переподключения
- **Недопустима потеря данных или повреждение исходных файлов**

### 7.3. Производительность

| Параметр | Требование |
|----------|-----------|
| **Интенсивность событий inotify** | До **1000 событий/сек** без потерь |
| **Нагрузка на CPU** | Не должно быть превышения заданной нагрузки (контроль через пул потоков) |

---

## 8. Требования к безопасности

### 8.1. Права доступа

| Файл | Права | Владелец |
|------|-------|----------|
| Конфигурационный файл | `644` | `root:root` |
| Служба | — | Рекомендуется запуск от специализированного системного пользователя `mh-compressor` (или `root` при необходимости доступа к системным папкам) |

### 8.2. Исполнение кода

> **⛔ Запрещено:** Выполнение внешних команд через `system()` или `popen()`. Сжатие только через API библиотек.

### 8.3. Изоляция

- Программа не должна записывать файлы в директории, куда нет прав на запись
- Программа не должна сжимать файлы, куда нет прав на чтение

### 8.4. Защита от symlink-атак

- Все файловые операции через `openat` с `O_NOFOLLOW`
- Проверка путей через `lstat`, не `stat`
- Функция `validate_filename()` для проверки имён файлов

---

## 9. Интеграция с системой (Systemd)

### 9.1. Тип службы

Демон (долгий процесс).

### 9.2. Файл юнита

`mh-compressor-manager.service`

### 9.3. Параметры юнита

| Параметр | Значение |
|----------|----------|
| **Type** | `notify` (предпочтительно) или `simple` |
| **KillMode** | `mixed` |
| **Путь установки** | `/usr/lib/systemd/system/` или `/etc/systemd/system/` |
| **Поддерживаемые команды** | `start`, `stop`, `restart`, `status` |

### 9.4. Завершение работы

При получении сигналов `SIGTERM`, `SIGINT` программа должна корректно завершить текущие задачи сжатия и освободить ресурсы.

### 9.5. Логирование службы

Вывод в `systemd-journald` (stdout/stderr).

---

## 10. Логирование и отчетность

### 10.1. Назначение

Системный журнал (`syslog` / `journald`), facility `daemon`.

### 10.2. Уровни логирования

```
INFO, WARNING, ERROR, DEBUG
```

### 10.3. Содержание логов

- Запуск и остановка службы
- Добавление/удаление каталогов мониторинга
- Начало и завершение сжатия (имя файла, размеры исходного и сжатого)
- Ошибки доступа, нехватки места, сбои библиотек
- Пропуск файлов меньше минимального порога (уровень `DEBUG`)

### 10.4. Коды возврата (Exit Code)

| Код | Значение |
|-----|----------|
| `0` | Успех |
| `1` | Ошибка выполнения |
| `2` | Ошибка конфигурации |

---

## 11. Требования к разработке и поставке

### 11.1. Система сборки

`CMake` или `Make`.

### 11.2. Зависимости

| Библиотека | Пакет (Fedora) |
|------------|----------------|
| **zlib** | `zlib-devel` (Fedora 38-42) / `zlib-ng-compat-devel` (Fedora 43+) |
| **Brotli** | `brotli-devel` |
| **systemd** | `systemd-devel` (заголовочные файлы) |
| **libstdc++** | Стандартная библиотека C++ |

### 11.3. Пакетирование

- Предоставление `SPEC` файла для сборки RPM пакета (Fedora)
- Возможность сборки из исходных кодов

### 11.4. Установка

- Создание системного пользователя и группы (опционально/рекомендуется)
- Копирование бинарника в `/usr/bin/`
- Размещение конфига по умолчанию в `/etc/mediahive/`
- **Служба не включается автоматически** (`enable` выполняется администратором вручную)

### 11.5. Документация

> **📚 Требование:** Наличие `README.md` с инструкцией по установке, настройке и управлению службой.

---

## 12. Критерии приемки

> **✅ Программа считается принятой при выполнении всех следующих условий:**

1. Программа компилируется на **Fedora 38+** без ошибок
2. Служба корректно стартует, останавливается и перезапускается через `systemctl`
3. Файлы с расширениями по умолчанию сжимаются в `.gz` и `.br`
4. При изменении исходного файла сжатые копии обновляются (с учетом `debounce`)
5. При удалении исходного файла сжатые копии удаляются
6. Конфигурация через файл и CLI аргументы работает корректно
7. Логи доступны через `journalctl -u mh-compressor-manager`
8. Потребление ресурсов не превышает заявленных лимитов
9. Файлы размером менее 256 байт не сжимаются (проверка через логи `DEBUG`)
10. Счётчики `files_skipped_small` и `bytes_skipped_small` корректно работают

---

## 13. История разработки

> **📜 Историческая справка:**
>
> Программа `mh-compressor-manager` версии 1.0.0 была разработана в **2026 году** специально для операционной системы **Fedora 43**.
>
> **Предыдущая версия (2014 год):**  
> Предыдущая версия утилиты сжатия была написана в **2014 году** и имела следующие ограничения:
> - Поддерживался только один алгоритм сжатия — **Gzip** (формат `.gz`)
> - Отсутствовала система журналирования событий (логов)
> - Не было поддержки многопоточности и асинхронной обработки
> - Отсутствовала интеграция с systemd
> - Конфигурация осуществлялась только через аргументы командной строки
> - Не поддерживалось сохранение метаданных (владелец, группа, SELinux-контекст)
>
> **Текущая версия (2026 год):**  
> Текущая версия полностью переработана с использованием современных стандартов (**C++23**) и учитывает все требования к надежности, безопасности и производительности.

### Сравнение версий

| Характеристика | Версия 2014 | Версия 2026 (текущая) |
|----------------|-------------|----------------------|
| Алгоритмы сжатия | Только Gzip (.gz) | Gzip + Brotli (.gz + .br) |
| Логирование | Отсутствовало | Полноценное (syslog/journald) |
| Многопоточность | Нет | Пул потоков |
| Интеграция с systemd | Нет | Type=notify, sd_notify() |
| Конфигурация | Только CLI | INI-файл + CLI |
| Минимальный размер файла | Не применялось | 256 байт (настраиваемо) |
| Идемпотентность | Нет | Проверка временных меток |
| Сохранение метаданных | Частичное | Полное (права, владелец, SELinux) |
| Стандарт C++ | C++98/03 | C++23 |

---

## 14. Правообладатель

> **📄 Информация о правообладателе**
>
> **© 2026 MediaHive.ru**  
> **Владелец:** ООО ОКБ "Улей"  
> **Автор:** Коваленко Павел  
> *Все права защищены.*

### Контактная информация

| Параметр | Значение |
|----------|----------|
| Проект | mh-compressor-manager |
| Версия | 1.0.0 |
| Стандарт | C++23 |
| Платформа | Linux (Fedora 43+, Fedora 38+, CentOS, Ubuntu) |
| Компилятор | GCC 13+ или Clang 16+ |
| Система сборки | CMake 3.20+ |
| Документация | README.md, README.html, TECHNICAL_SPECIFICATION.md |

---

## 📚 Приложения

### A. Связанные документы

- **Исследование минимального размера файла:** `Исследование_минимальный_размер_файла_gzip_brotli.md`
- **Правила кода:** `RULES.md`
- **Сборка и деплой:** `DEPLOY.md`
- **Pull Request и коммиты:** `CONTRIBUTING.md`

### B. Контрольный список реализации минимального размера

- [ ] Добавить константы `MIN_COMPRESS_SIZE` и `OPTIMAL_COMPRESS_SIZE` в конфигурацию
- [ ] Реализовать проверку `should_compress(file_size)` до начала сжатия
- [ ] Добавить логирование на уровне `DEBUG` для пропущенных файлов
- [ ] Добавить счётчики `files_skipped_small` и `bytes_skipped_small`
- [ ] Обновить документацию конфигурационного файла
- [ ] Написать unit-тесты для проверки порогов
- [ ] Протестировать на реальных данных (loadtest)

---

**© 2026 MediaHive.ru** | ООО ОКБ "Улей" | Автор: Коваленко Павел  
Техническая спецификация версия 3.0 | Документ утвержден в 2026 году
